# Pickle run: the Workshop gallery, first run of feature 14, pass vitrine, 2026-09-25

- **exitReason:** `failed`, `setName` `vitrine`. Exit code 1 through the TicketDispatcher (request 3b3b): 4 scenarios discovered, 4 played, 3 passed, 1 failed. The first run of the ScreenshotStudio fixture with this mod: it loads, the cells (154,98) for the stand and (149,98) for Miel are free, the studio steps resolve, nothing was undefined.
- **The failure:** `gallery 04: the ground lit at night as the rocket leaves`: "after 60 s the stand's light never came on". The capture of that moment is dark, with Miel at the centre of the screen next to the (unlit, hence invisible) stand and "Dark (0%)": she had arrived, and the light did not come on within 60 s. Not yet explained: a slow machine, or a watcher that does not fire at night in this fixture. The rerun adds `"Miel" is watching the stand` before waiting for the light, whose failure prints the trace of her jobs.
- **What the images say (opened by the session):**
  - `gallery 01` (the fuse) and `02` (the launch): the studio meadow, Miel standing five cells west, the stand small in the middle; a dark grey column of smoke above the rack in 01, the rocket's red trail going up and the column in 02. Good pictures of the mod in a green meadow, with no skeleton and no HUD. The stand is small in the frame at the closest zoom: a crop will do.
  - `gallery 03` (the burst): **nothing to see**. Taken at the widest zoom, the whole studio, the stand a speck and no burst. The rerun takes three pictures at one zoom step out, the camera four cells north, 50 ticks apart.
  - `gallery 05` (the reload): the interface is there, the stand selected with its watching areas and its "Fuel 3 / 10" gizmo, but **the inspect pane is missing**: ClearScreen drops it as a window. The rerun lifts the suppression before selecting. The right edge shows alerts and six "Area revealed" letters of the studio.
  - `gallery 06` (the blueprint): the ghost of the stand and "Firework stand (blueprint)" at the bottom left, in the meadow. The same letters and alerts on the right.
- **Evidence, on disk and ignored by git:** `Tests/Pickle/runs/2026-09-25-vitrine/` keeps `summary.json`, `junit.xml` and the six images (jpeg). Deleted: the html report, the log, the messages file. To be deleted once the rerun replaces it.
