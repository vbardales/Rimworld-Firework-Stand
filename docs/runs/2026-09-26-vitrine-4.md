# Pickle run: the Workshop gallery, feature 14, pass vitrine, 2026-09-26 (fourth request)

- **exitReason:** `failed`, `setName` `vitrine`. Exit code 1 through the TicketDispatcher (request a79b): 4 scenarios played, 3 passed, 1 failed. The request before it (98c7) was cut by the machine shutting down for the night (exit 1073807364, `0x40010004`): no verdict, no evidence. The launcher archived this run in `pickle-reports-archive/0926-1636` (the archive `0926-1631` the dispatcher named belongs to another mod's run), deleted after this copy.
- **The failure, third time, and now narrower:** `gallery 04: the ground lit at night as the rocket leaves`: "after 30 s the stand's light never came on", in 76 s. The steps before it passed: `Miel is watching the stand`, and `the stand ... comes to hold 0 launchers` (the launcher **is spent, so the stand fired**). The capture taken at the failure shows fireworks streaks over the stand and dark ground. So the shot happens and the light is not seen by `Glows`.
- **A hypothesis checked and refuted:** that the game's `CompGlower` refuses to light a building whose `CompRefuelable` is empty (the last rocket spent its launcher). Read off the compiled game (IL of every `CompGlower` method): none of them touches `CompRefuelable`; `ShouldBeLitNow` asks `Spawned`, `FlickUtility.WantsToBeOn` and every `IThingGlower` comp, as the functional test already says. Feature 05 passes with two launchers at game speed `fast`; this scenario runs at `normal` with one launcher in the studio.
- **Next step:** the step `the light ... comes on within N seconds` now traces the stand's own state while it waits (`lastShotTick`, `departed`, `lit`, `Glows`, game tick and speed) and prints it in the failure message, so the next run names the missing link instead of guessing. Steps DLL rebuilt.
- **What the images say (opened by the session):**
  - `gallery 01` (fuse): **a dark puff of smoke is visible above the rack**, the thickened one. The stand is small in the frame (the picture needs a crop).
  - `gallery 03b` (burst by day): still a few blue streaks near the watcher: a burst does not read by day.
  - `gallery 04` (failure capture): night, the studio's HUD and alerts showing (presentation mode is not applied in this scenario), the streaks above the stand, no light on the ground.
  - `gallery 02`, `03a`, `03c`, `05`, `06`: not reopened this run; same scenarios, same staging as `2026-09-25-vitrine-2`.
- **Evidence, on disk and ignored by git:** `Tests/Pickle/runs/2026-09-26-vitrine-4/` keeps `junit.xml` and the eight images (jpeg). Deleted as superseded: `2026-09-25-vitrine-2` and the empty `2026-09-25-vitrine-3`.
