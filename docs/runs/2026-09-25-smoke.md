# Pickle run: the smoke scenarios on the fuse's own fleck, 2026-09-25

- **exitReason:** `passed`. Exit code 0, through the TicketDispatcher (request 5241), `-Filter '::a thread of smoke at the foot of the stand'`: the filmed scenario and its stills twin, 2 of 2 passed, on d51b7dd (`FS_FuseSmoke`, fade-in 0.08 s, size 0.6, a puff every 8 ticks, at least 2 near the stand at 25 ticks).
- **What the pictures say (opened by the session):** the fuse still at 25 ticks shows only a faint smudge above the stand: the smoke is **now there but still barely visible**. The 70-tick still shows a light haze and sparks. The film ended before the rocket left, so it shows no fuse either.
- **Consequence:** the puffs are darker (grey tint), larger (size 0.9) and thicker (every 5 ticks), the still is taken at 40 ticks with at least 4 puffs asserted (6c81af6); rerun requested (374b). This run's evidence is deleted once that one replaces it.
- **Evidence, on disk and ignored by git:** `Tests/Pickle/runs/2026-09-25-smoke/` keeps `summary.json`, `junit.xml`, the 2 stills (jpeg) and the film.
