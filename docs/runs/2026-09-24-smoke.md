# Pickle run: the smoke scenarios on the game's own Smoke fleck, 2026-09-24

- **exitReason:** `passed`. Exit code 0, through the TicketDispatcher (request 8c21), `-Filter '::a thread of smoke at the foot of the stand'`: the filmed scenario and its stills twin, 2 of 2 passed, on c8c9e03 (steps fixed).
- **What the counts say:** the stills scenario asserted at least two smoke puffs near the stand 25 ticks into the fuse, and passed: the puffs **are thrown** on the build with `CompTick`. That closes "never thrown".
- **What the pictures say (opened by the session):** the still at 25 ticks shows **no smoke** at the foot of the stand; the still at 70 ticks shows a faint haze and sparks; the film shows nothing before the launch, then a red streak, a flash and a haze in its last three frames. The 25-tick count and the empty capture together say "thrown and invisible", and the cause is the game's Smoke fleck: `fadeInTime` 0.5 s against a fuse of one second (60 ticks).
- **Consequence:** the fuse smoke now uses its own fleck (`FS_FuseSmoke`, fade-in 0.08 s), d51b7dd; the smoke pass is rerun on it. This run's evidence (one film, two stills, not opened by the owner) is deleted once that run replaces it.
- **The film is mostly a loading screen:** 17 of its 26 seconds are the main menu and the load, the game shows for the last nine.
