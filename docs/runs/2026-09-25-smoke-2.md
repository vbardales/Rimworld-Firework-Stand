# Pickle run: the smoke scenarios on the darker fuse smoke, 2026-09-25 (first attempt)

- **exitReason:** `failed`. Launcher exit code 1, a verdict, through the TicketDispatcher (request 374b, replayed after a worker was stopped by mistake at 16:03; the staging is the new worker's). 2 scenarios, 1 passed, 1 failed, on 6c81af6 (grey tint, size 0.9, a puff every 5 ticks, still at 40 ticks asserting at least 4 puffs).
- **The failure is the test's, not the mod's:** the stills scenario stopped at `Watcher is at (135,151) after being placed`, one cell from the (135,150) it was told to, with the same cell exact in the run before. The step `Firework Stand: {string} is placed at x= z=` now accepts a cell within one cell (21d4b8a); the stills scenario never reached the fuse still, so **nothing is known yet from a still at 40 ticks**.
- **What the film shows (opened by the session, filmed scenario passed):** for the fuse itself nothing readable at the film's size; after the launch, a red streak, a flash and a **dark grey smoke** clearly visible over the stand for the last frames. So the tinted smoke is visible; whether it is visible *during the fuse* is what the rerun's still has to say (the film's frame rate does not settle it).
- **Evidence, on disk and ignored by git:** `Tests/Pickle/runs/2026-09-25-smoke-2/` keeps `summary.json`, `junit.xml` and the film. Deleted: the html report, the log, the messages file, the failure still. The previous smoke folder (`2026-09-25-smoke`, the first tuning) is deleted, superseded by this build.
- **Rerun:** request 50ce, the stills scenario only.
