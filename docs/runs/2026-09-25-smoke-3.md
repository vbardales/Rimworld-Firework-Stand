# Pickle run: the smoke stills, second tuning, 2026-09-25

- **exitReason:** `passed`. Exit code 0 through the TicketDispatcher (request 50ce), `-Filter '::stills: a thread of smoke at the foot of the stand'`: 1 scenario discovered, 1 played, 1 passed, on 21d4b8a (the same mod DLL as 6c81af6: grey tint, size 0.9, a puff every 5 ticks, still at 40 ticks asserting at least 4 puffs; only the placement step of the test changed).
- **What the pictures say (opened by the session):** the fuse still (40 ticks into the fuse) shows **a faint dark smudge in the rocket tips and nothing readable above them**: the count says at least 4 puffs are there, the picture does not show a thread of smoke. The 70-tick still shows a light purple-grey haze around the stand. Still too faint.
- **Consequence:** the smoke was spawned in the middle of the rack, over the striped rockets, which hide it. It now rises from the top of the rack, larger at the start, black-grey and opaque, at 1.0 to 1.4 cells a second and straight up (8b2096f); rerun requested (request 189a). The evidence of this run is deleted once that one replaces it.
- **Evidence, on disk and ignored by git:** `Tests/Pickle/runs/2026-09-25-smoke-3/` keeps `summary.json`, `junit.xml` and the two stills (jpeg). The launcher's archive of this run (`pickle-reports-archive/0925-1710`, 2.1 GB) was deleted at the dispatcher's suggestion, after checking that it held the same one scenario and no `keep.txt`.
