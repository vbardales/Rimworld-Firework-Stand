# Pickle run: the smoke stills, smoke rising from the top of the rack, 2026-09-25

- **exitReason:** `passed`. Exit code 0 through the TicketDispatcher (request 189a), `-Filter '::stills: a thread of smoke at the foot of the stand'`: 1 scenario discovered, 1 played, 1 passed, on the mod DLL of 8b2096f (smoke spawned at the top of the rack, black-grey and opaque, straight up at 1.0 to 1.4 cells a second, size 1.2 to 1.8, a puff every 5 ticks; the still at 40 ticks asserts at least 4 puffs). The tree changed after the request was queued, by text and data only, not the DLL: the fuel label capitals (`f2ccb52`), the stand's drawn size, and the new feature 14 and its map.
- **What the pictures say (opened by the session):** the fuse still, 40 ticks in, shows **a dark grey puff rising above the rack**, clearly visible against the ground; the 70-tick still shows the rocket's red trail leaving upward and **a thicker dark smoke column over the stand**. This is the first pass on which the smoke can be seen on a still. Not yet shown to the owner: it is her validation.
- **Evidence, on disk and ignored by git:** `Tests/Pickle/runs/2026-09-25-smoke-4/` keeps `summary.json`, `junit.xml` and the two stills (jpeg). The previous smoke-stills folder (`2026-09-25-smoke-3`) is deleted, superseded.
