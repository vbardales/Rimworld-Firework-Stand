# Pickle run: the Workshop gallery, rerun of feature 14, pass vitrine, 2026-09-25 (second run)

- **exitReason:** `failed`, `setName` `vitrine`. Exit code 1 through the TicketDispatcher (request 5a79): 4 scenarios played, 3 passed, 1 failed. Replaces the first run (`2026-09-25-vitrine`, folder deleted, its summary kept in git history).
- **The failure is the same, and it is reproducible:** `gallery 04: the ground lit at night as the rocket leaves`: "after 60 s the stand's light never came on", in 84 s (not the watchdog, which is 300 s now). This time the step `Miel is watching the stand` passed before it (the capture shows her on her watch cell, some ten cells from the stand), so **the watcher was at the stand and the light still did not come on in 60 s**. Whether the stand fired at all is not known: the scenario did not assert it. The next run asserts the launcher is spent first, then waits for the light for 30 s, which tells "does not fire at night in this fixture" from "fires, and the light does not come on". The same feature 05-light passes at night in the test colony, so the mod's light is not broken there.
- **What the images say (opened by the session):**
  - `gallery 03a, 03b, 03c` (the burst, one zoom step out, camera four cells north, 50 ticks apart): **the bursts are there but tiny, a few blue streaks near the watcher** in a daylight meadow. A burst does not read by day. It goes to the night scenario, where the light and the burst are the picture.
  - `gallery 05` (the reload): **the inspect pane is there now**: "Firework stand", "Fireworks loaded: 3 / 10" (with its capital), "Reloading: 0.3 hours", the "Fuel 3 / 10" gauge, the watching areas drawn round the stand. The right edge still shows the studio's alerts and six "Area revealed" letters: to crop.
  - `gallery 01`, `02`, `06`: as in the first run.
- **Evidence, on disk and ignored by git:** `Tests/Pickle/runs/2026-09-25-vitrine-2/` keeps `summary.json`, `junit.xml` and the eight images (jpeg). The first run's folder is deleted.
